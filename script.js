// le code du  menu burger  

const burger = document.getElementById('burger');
const navLinks = document.getElementById('navbar-links');

burger.addEventListener('click', () => {
  navLinks.classList.toggle('active');
});































